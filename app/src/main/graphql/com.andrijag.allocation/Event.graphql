query Event($id: String!) {
    event (id: $id) {
        id,
        title,
        description,
        location,
        clientName,
        start,
        end,
        canStart,
        canStop
    }
}
